{
  "name": "fast-cv",
  "version": "0.1.0",
  "description": "Fast Code Validation — orchestrates linters and security scanners with unified Markdown reports",
  "type": "module",
  "bin": {
    "fast-cv": "./bin/fast-cv.js"
  },
  "scripts": {
    "test": "node --test test/*.test.js test/tools/*.test.js"
  },
  "keywords": ["linter", "security", "scanner", "cli", "code-quality"],
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "ignore": "^6.0.2",
    "yaml": "^2.6.1"
  }
}
